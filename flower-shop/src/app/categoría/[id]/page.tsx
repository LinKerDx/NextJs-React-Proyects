import SectionContainer from "@/app/components/SectionContainer"


export default async function ProductosLista({ params }: { params: { id: string } }) {
    const { id } = params

    return (
        <SectionContainer>
            hola
        </SectionContainer>
    )

}

